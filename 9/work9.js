let john={name:"John",surname:"Smith",id:1}
let pete={name:"Pete",surname:"Hunt",id:2}
let mary={name:"Mary",surname:"Key",id:3}
let users=[john,pete,mary];
// let usersMapped= users.map(user=>({fullName:`$(user.nasm)$(user.surname)`,id:user.id})
// alert(uersMapped[0].id)
// alert(usersMapped[0].fullName)

function myarr(fullName,id){
    let arr=
    {
        id:users[i].id
        getfullName:function(){//为什么这个不行呢？实在是做不完了
            return fullName=users[i].name+""+users[i].surname;
        }
    }
    
}
alert(uersMapped[0].id)
alert(usersMapped[0].fullName)